---
topic: xgplib-workers
updated: 2026-08-26T01:46
---

- (decision) Create SPEC.md based on PRD and ARCHITECTURE-SPINE docs in _bmad-output/planning-artifacts/
- (constraint) Use Hexagonal Architecture (Ports and Adapters)
- (constraint) Use Python's native abc.ABC for Ports
- (constraint) Use Dependency Injection container framework
- (constraint) Use standard stack: SQLAlchemy, Pydantic, Ruff
- (constraint) PostgreSQL with JSON/JSONB fields for metadata
- (constraint) Use explicit custom Domain Exceptions for error handling
- (constraint) Use Test-Driven Development (TDD)
- (constraint) Must implement circuit breakers and retries for external APIs
- (capability) Fetch Xbox Game Pass catalog from Microsoft via scheduled cron (hourly) and identify added/removed titles. Success: A complete list of currently available and recently removed titles is generated hourly.
- (capability) Query IGDB API for exact title matches to retrieve core metadata. Success: Titles matching IGDB have their metadata enriched (cover, description, etc.).
- (capability) Handle IGDB match failures by storing raw data and logging for manual review. Success: Unmatched titles are persisted without metadata and flagged for intervention.
- (capability) Persist catalog updates to database using upserts and soft-deletes (inactive flags). Success: The database accurately reflects the current Microsoft catalog state and retains history of removed games.
- (question) What are the exact, officially documented Microsoft API endpoints for catalog extraction?
- (assumption) Manual resolution of unmatched titles will be done via direct SQL scripts initially.
- (event) Self-Validation Pass 1 (Coherence): Passed. Capabilities have intent and success, constraints bend decisions, non-goals are explicit, and prose is lean.
- (event) Self-Validation Pass 2 (Preservation): Passed. All load-bearing claims from PRD and Architecture Spine have been preserved in SPEC.md.
- (decision) Resolved open question regarding Microsoft endpoints: The worker will use catalog.gamepass.com to fetch IDs (using specific list IDs for Console, PC, and EA Play) and then hydrate the game metadata using displaycatalog.mp.microsoft.com in batches of 200 to avoid URL length limits.
