---
scope: project
purpose: build substrate
altitude: system
updated: 2026-08-26T00:24
---

- (direction) User requested standard industry practices for Hexagonal Architecture boundaries and dependency injection in Python.
- (constraint) Hexagonal Architecture (Ports and Adapters) is the core paradigm.
- (decision) Binds: Dependency Rule Enforcement. Prevents: Architectural drift where domain logic implicitly couples to infrastructure. Rule: Boundaries are enforced by folder convention (Domain, Application, Infrastructure) and validated via static analysis tools (e.g., Ruff/Flake8, Black for styling) and Code Review, prioritizing convention over strict architectural testing frameworks for now.
- (decision) Binds: Dependency Injection Strategy. Prevents: Hardcoded concrete infrastructure dependencies inside application use cases, complicating testing and flexibility. Rule: Dependency Injection is orchestrated via a DI Container framework (Option Y) rather than manual wiring, standardizing how components receive their dependencies as the project scales.
- (direction) User requested standard industry practices for Python Ports definition (Interfaces) and State Mutation/Use Case orchestration.
- (decision) Binds: Port (Interface) Definition Standard. Prevents: Inconsistent contract definitions or runtime failures from incomplete structural typing. Rule: Ports (interfaces for infrastructure) must be defined using Python's native `abc.ABC` (Abstract Base Classes) to enforce explicit implementation and fail-fast instantiation of adapters.
- (decision) Binds: Use Case State Mutation Flow. Prevents: Overly complex functional workarounds or scattered state changes. Rule: Use Cases (Application layer) are responsible for orchestrating state mutation imperatively. They retrieve entities, invoke domain logic, and explicitly call repository/port methods (e.g., `save`) to persist state.
- (decision) Binds: Error Handling Strategy. Prevents: Infrastructure-level errors polluting the domain, or domain logic being bogged down by Result monad unboxing. Rule: Domain and Application layers raise explicit, custom Domain Exceptions (e.g., `DomainError`, `EntityNotFound`). The outermost layer (Adapters/Entrypoints) is responsible for catching these and translating them into the appropriate infrastructure response (e.g., specific logs, queue NACKs, HTTP codes).
- (direction) User requested binding common industry-standard libraries as the foundational stack for the architecture (e.g. SQLAlchemy, Pydantic).
- (decision) Binds: Foundational Stack. Prevents: Tooling sprawl and reinventing the wheel with non-standard libraries. Rule: Adopt industry-standard Python tooling: SQLAlchemy for ORM (Infrastructure layer persistence), Pydantic for DTOs and configuration validation, and Ruff for fast, unified linting and formatting.
- (event) spine finalized
